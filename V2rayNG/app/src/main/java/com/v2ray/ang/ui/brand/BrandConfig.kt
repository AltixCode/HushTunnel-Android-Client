package com.v2ray.ang.ui.brand

/**
 * Single place to point this white-label client at your backend.
 *
 * TODO: replace with your own domain once you have one. The Vercel preview
 * alias below works today but will change if that deployment is ever
 * deleted/recreated — a custom domain is the only permanent option.
 */
object BrandConfig {
    const val API_BASE_URL = "https://hushtunnel.com"
}
